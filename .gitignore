# Obsidian Git 同步 - .gitignore

## 排除目录（这些不需要同步）
.obsidian/plugins/              # 插件目录（包含嵌入式仓库和可能的密钥）
.obsidian/snippets/             # 自定义 CSS
.obsidian/cache/                # 缓存目录
.obsidian/log/                  # 日志目录
.claudian/                      # Claude 会话历史

## 排除文件
.DS_Store                       # macOS 系统文件
*.log                           # 日志文件
*.tmp                           # 临时文件
*.swp                           # Vim 临时文件
*~                              # 备份文件

# 排除包含密钥的文件（可能有敏感信息）
# .obsidian/plugins/remotely-save/  # 远程同步插件（可能包含 OAuth 密钥）

## ⚠️ 注意
# 如果你需要同步插件配置（主题、插件列表等），可以将以下行注释掉：
# .obsidian/community-plugins.json
# .obsidian/workspace.json
# .obsidian/themes/